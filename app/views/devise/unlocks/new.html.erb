<%= render "devise/shared/auth_wrapper",
           title: "ロック解除メールの再送",
           subtitle: "アカウントがロックされた場合は、こちらから解除メールを送信できます。" do %>
  <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "space-y-4" }) do |f| %>
    <div class="flex flex-col gap-2">
      <%= f.label :email, "Eメール", class: "text-sm font-semibold text-base-content" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "input input-bordered w-full rounded-2xl border-base-200 bg-base-100" %>
    </div>

    <div class="pt-2">
      <%= f.submit "解除メールを送信", class: "btn btn-primary w-full" %>
    </div>
  <% end %>
<% end %>
