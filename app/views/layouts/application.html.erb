<!DOCTYPE html>
<html lang="ja" data-theme="light">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Instant Web Video Edit</title>
  <meta name="description" content="YouTube 動画をその場で編集し、タイムスタンプ単位でクリップ保存。">
  <%= csrf_meta_tags %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
  <%= javascript_importmap_tags %>
</head>

<body class="page-gradient min-h-screen text-base-content antialiased <%= user_signed_in? ? 'logged-in' : 'logged-out' %>">
  <div class="flex min-h-screen flex-col">
    <%= render 'layouts/header' %>

    <main class="flex flex-1 py-3">
      <div class="mx-auto flex flex-1 justify-center max-w-[110rem] flex-col px-4 sm:px-6 lg:px-10">
        <%= render 'layouts/flash_message' %>
        <%= yield %>
      </div>
    </main>

    <%= render 'layouts/footer' %>
  </div>
</body>
</html>
